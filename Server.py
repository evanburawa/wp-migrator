class Server:
    
    def __init__(self, config):
      self.config = config

      self.host = config.remote_server_ip
      self.user = config.remote_server_user
      self.pw = config.remote_server_pw
      self.html_dir = config.remote_server_html_dir

      print('Server loaded')
    
    def set_username(username):
      return
    
    def set_host(host):
      return

    def set_password(password):
      return

    def set_local_dir(dir):
      return

    def connect():
      return


    def upload_files():
      return